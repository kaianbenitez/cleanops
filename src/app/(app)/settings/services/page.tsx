"use client";

import Link from "next/link";
import { useEffect, useState } from "react";

type Service = { id: string; name: string; description: string | null; defaultPriceCents: number; defaultDurationMinutes: number; isActive: boolean };
const dollars = (cents: number) => (cents / 100).toFixed(2);

export default function ServiceCatalogPage() {
  const [services, setServices] = useState<Service[]>([]); const [loading, setLoading] = useState(true); const [newName, setNewName] = useState(""); const [newPrice, setNewPrice] = useState("0"); const [newDuration, setNewDuration] = useState("120"); const [message, setMessage] = useState("");
  async function load() { setLoading(true); const data = await fetch("/api/services?all=1").then((response) => response.json()); setServices(data.services ?? []); setLoading(false); }
  // eslint-disable-next-line react-hooks/set-state-in-effect -- load server-backed settings on mount
  useEffect(() => { load(); }, []);
  async function saveField(id: string, fields: Record<string, unknown>) { const response = await fetch(`/api/services/${id}`, { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify(fields) }); setMessage(response.ok ? "Service saved." : "Could not save service."); await load(); }
  async function addService() { if (!newName.trim()) return; const response = await fetch("/api/services", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ name: newName, defaultPriceCents: Math.round(Number(newPrice || 0) * 100), defaultDurationMinutes: Number(newDuration || 0) }) }); if (!response.ok) { setMessage("Could not add service."); return; } setNewName(""); setNewPrice("0"); setNewDuration("120"); setMessage("Service added."); await load(); }
  if (loading) return <div className="co-card p-8 text-sm text-[var(--co-muted)]">Loading service catalog…</div>;
  return <div className="space-y-6"><header className="flex flex-wrap items-end justify-between gap-3"><div><p className="eyebrow">Settings / Operations</p><h1 className="page-title mt-2">Service catalog</h1><p className="page-subtitle">Manage the services available when creating one-off jobs. Quote pricing is configured separately.</p></div><Link href="/settings" className="co-button-secondary">← Settings</Link></header><section className="co-card overflow-hidden"><div className="flex items-center justify-between border-b border-[var(--co-line-soft)] px-5 py-4"><div><p className="eyebrow">Job defaults</p><h2 className="mt-1 text-lg font-semibold">Available services</h2></div><span className="text-xs text-[var(--co-muted)]">{services.filter((service) => service.isActive).length} active</span></div><div className="divide-y divide-[var(--co-line-soft)]">{services.map((service) => <div key={service.id} className={`p-5 ${!service.isActive ? "opacity-60" : ""}`}><div className="flex flex-wrap items-start gap-3"><input defaultValue={service.name} onBlur={(event) => event.target.value !== service.name && saveField(service.id, { name: event.target.value })} className="co-input min-w-[220px] flex-1 font-semibold" /><button onClick={() => saveField(service.id, { isActive: !service.isActive })} className="co-button-secondary text-xs">{service.isActive ? "Deactivate" : "Activate"}</button></div><textarea defaultValue={service.description ?? ""} onBlur={(event) => event.target.value !== (service.description ?? "") && saveField(service.id, { description: event.target.value || null })} placeholder="Description (optional)" rows={2} className="co-input mt-3 w-full resize-none" /><div className="mt-3 grid gap-3 sm:grid-cols-2"><label className="block text-xs font-semibold text-[var(--co-muted)]">Default price<input type="number" min="0" step="0.01" defaultValue={dollars(service.defaultPriceCents)} onBlur={(event) => saveField(service.id, { defaultPriceCents: Math.round(Number(event.target.value || 0) * 100) })} className="co-input mt-1 w-full" /></label><label className="block text-xs font-semibold text-[var(--co-muted)]">Duration (minutes)<input type="number" min="0" step="15" defaultValue={service.defaultDurationMinutes} onBlur={(event) => saveField(service.id, { defaultDurationMinutes: Number(event.target.value || 0) })} className="co-input mt-1 w-full" /></label></div></div>)}{services.length === 0 && <p className="p-10 text-center text-sm text-[var(--co-muted)]">No services yet. Add your first job preset below.</p>}</div></section><section className="co-card p-5"><p className="eyebrow">Add service</p><h2 className="mt-1 text-lg font-semibold">Create a job preset</h2><div className="mt-4 grid gap-3 sm:grid-cols-[1fr_150px_170px_auto]"><input className="co-input" value={newName} onChange={(event) => setNewName(event.target.value)} placeholder="Standard weekly clean" /><input className="co-input" type="number" min="0" step="0.01" value={newPrice} onChange={(event) => setNewPrice(event.target.value)} placeholder="Price" /><input className="co-input" type="number" min="0" step="15" value={newDuration} onChange={(event) => setNewDuration(event.target.value)} placeholder="Minutes" /><button onClick={addService} className="co-button-primary">Add service</button></div>{message && <p className="mt-3 text-sm font-medium text-[var(--co-evergreen)]">{message}</p>}</section></div>;
}
