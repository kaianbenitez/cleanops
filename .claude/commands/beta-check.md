Audit CleanOps for beta readiness.

Inspect the current git diff and run `npm run verify`.
Check quote creation/public proposal/acceptance, My Day state transitions, protected routes, company scoping, cron authorization, GHL retry visibility, and backup workflow configuration.
Use read-only GHL checks against the test location only. Never mutate contacts, tags, workflows, payments, or production data.
Return: blockers, warnings, passed checks, and the next three fixes.
