Perform a focused quote regression audit.

Trace the flow from `/quotes/new` to `/api/quotes`, `/quotes/[quoteId]`, `/api/quotes/[quoteId]/send`, `/quote/[token]`, and public acceptance.
Verify service-area blocking, tier selection, add-on total recalculation, insurance/W-9 links, quote conversion, and no room-by-room price breakdown in the public proposal.
Run typecheck/build after any requested fix. Do not rebuild the quote architecture.
