# CleanOps Risk Artifact

## Audit update — 2026-08-25

This repo-local file is the operational risk queue for the `risk-remediation` skill. Populate and maintain it from verified codebase evidence. Do not require access to an external artifact to use it.

## Audit coverage

| Order | Category | Status | Last audited | Scope / outcome |
|---|---|---|---|---|
| 1 | Auth, authorization, IDOR, and company/tenant isolation | Audited | 2026-08-25 | Inspected `src/proxy.ts`, `src/lib/supabase/middleware.ts`, `src/lib/auth/current-user.ts`, `src/lib/auth/field-staff.ts`, `(app)/layout.tsx`, `settings/layout.tsx`, all 45 dynamic (`[id]`) API routes, all top-level list/create API routes, the two public-token routes, and the employee-scoped job-action routes (clock-in/out, transition, mileage, photos). Tenant isolation is consistently enforced (every checked query scopes by `companyId`, every mutating dynamic route re-verifies parent ownership before acting on a nested ID, `jobAssignments` inserts always validate `employeeIds` against the acting admin's company). Found one functional access-control bug (customer feedback link is blocked by middleware) and one authorization-layering gap (Settings/Supplies pages reachable by non-admins, though the underlying API still blocks them). See Open findings. |
| 2 | API validation, uploads, webhooks, secrets, SSRF, XSS, and SQL injection | Audited | 2026-08-25 | Inspected both webhook handlers, all file-upload routes, every `dangerouslySetInnerHTML`/`sql.raw` usage repo-wide, outbound `fetch()` calls for SSRF, secret handling (`NEXT_PUBLIC_*` vars, git history for committed `.env` files), the public lead-capture endpoint, and — prompted by `HANDOFF.md`'s own account of a prior identical incident — cross-checked every table in `schema.ts` against every `ENABLE ROW LEVEL SECURITY` statement in `drizzle/*.sql`. Found two Red issues (one a repeat of a previously-fixed class of bug); the Square webhook one is fixed (see Resolved). Uploads, SSRF, XSS, hardcoded secrets, and SQL injection all came back clean. See Open findings. |
| 3 | Database safety, migrations, backups, data loss, and concurrency | Audited | 2026-08-25 | Ran `npm run check:drift` against the hosted DB (the project's own standard tool for this), audited every migration for destructive statements (`DROP TABLE`/`COLUMN`, `CASCADE`, forced `NOT NULL`), checked FK `onDelete` behavior across `schema.ts` (none use cascade — every FK defaults to restrict), reviewed every `db.delete()`/`tx.delete()` call site in `src/app/api` for pre-delete safety checks, checked `SUPABASE.md` for backup/recovery documentation, and spot-checked write flows for check-then-act races (invoice send, quote accept, clock-in/transition). `check:drift` surfaced an active production bug (see Open findings, Red). Delete flows are unusually careful — employee hard-delete and room-type delete both check for historical references first and fall back to soft-delete/refuse. No documented backup/PITR policy exists in-repo (Yellow, needs a human to check the Supabase dashboard, not verifiable from code). One minor check-then-act race in invoice sending, mitigated in the real (non-mock) path by Square's own idempotency key (Green). |
| 4 | External integrations and silent mock/fallback behavior | Audited | 2026-08-25 | Reviewed the mock-mode implementations for GHL (`src/lib/ghl/client.ts`), Square (`src/lib/square/client.ts`), and email (`src/lib/email/send.ts`); traced how `squarePublicUrl` (the mock invoice link) reaches customers via the feedback flow; checked whether the Settings → Integrations page and the admin-facing invoice/quote UIs disclose mock state; and checked what happens to inbound webhook processing errors (both GHL and Square) versus outbound GHL sync errors, which have a dedicated `/sync-issues` admin page. Email correctly fails loud (throws) when unconfigured rather than mocking. GHL and Square both mock cleanly and consistently, and outbound GHL sync failures are visible via `/sync-issues`. Found two Yellow gaps: a real customer-facing broken link in Square's current mock mode, and inbound webhook errors (GHL lead capture + Square payments) that are recorded but never surfaced to any admin UI. |
| 5 | Production configuration, CI, monitoring, and deployment readiness | Not started | — | — |
| 6 | Reliability, dates/timezones, and privacy | Not started | — | — |
| 7 | Performance, unbounded queries, and third-party API cost | Not started | — | — |

## Open findings

| Priority | Status | Finding | Evidence | Remediation / next action | Validation | Updated |
|---|---|---|---|---|---|---|
| Red | Open — active production bug, migration written but not yet applied to hosted DB | `schema.ts` expects `quotes.desired_cleaning_date`, but the hosted database does not have that column — `npm run check:drift` fails on it right now. Every query that selects this column throws a live Postgres error, which affects the internal Quotes list page, the quote detail page, and — most importantly — the public customer-facing quote-acceptance flow (`/quote/[token]`, `/api/public/quotes/[token]`, `/api/public/quotes/[token]/accept`). This is the exact failure mode `AGENTS.md` warns about ("a committed migration file is not evidence it was applied... this has bitten the project three times") happening again, live. | `npm run check:drift` output: "Missing COLUMNS (schema.ts expects these; the database does not have them): - quotes: desired_cleaning_date". The column is defined in `src/db/schema.ts:417` and referenced in `src/app/(app)/quotes/page.tsx`, `src/app/(app)/quotes/[quoteId]/page.tsx`, `src/app/quote/[token]/page.tsx`, and `src/app/api/public/quotes/[token]/route.ts` + `/accept/route.ts`. The matching migration, `drizzle/0044_quote_desired_cleaning_date.sql`, is a single additive statement: `ALTER TABLE "quotes" ADD COLUMN IF NOT EXISTS "desired_cleaning_date" date;` — committed to the repo but never applied to the hosted DB. | Apply `drizzle/0044_quote_desired_cleaning_date.sql` by hand to the hosted DB in a single transaction (low risk: purely additive, nullable, `IF NOT EXISTS`, no data touched, does not require app downtime). Then re-run `npm run check:drift` and confirm it passes clean. Attempted in-session; blocked by the harness's own permission classifier as a database-mutating action needing a human in the loop — same as the RLS migration below. **Still needs a human to run it.** | Re-run `npm run check:drift` after applying — it should report no missing columns. Then click through `/quotes`, a quote detail page, and a public `/quote/[token]` link to confirm no more server errors on those pages. | 2026-08-25 |
| Red | Open — migration merged to `main` (`9c366c1`), not yet applied to hosted DB | 4 tables added to `schema.ts` after the 2026-08-04 RLS sweep were never given `ENABLE ROW LEVEL SECURITY` — the exact same class of bug `HANDOFF.md` documents finding and fixing for 27 other tables on 2026-08-04, where it meant "the hosted DB was openly readable by anyone with the public anon key" via Supabase's auto-exposed PostgREST REST API (`<project>.supabase.co/rest/v1/<table>`), no login required, using `NEXT_PUBLIC_SUPABASE_ANON_KEY` which ships inside the app's own browser bundle by design. | Tables: `pto_requests` (`src/db/schema.ts:133`), `employee_service_locations` (`:340`), `payroll_job_reviews` (`:671`), `feedback_requests` (`:691`) — created in `drizzle/0034_pto_requests.sql`, `0041_unify_employee_service_areas.sql`, `0033_payroll_driver_trainer_reviews.sql`, `0031_feedback_requests.sql` respectively. Grepped every `drizzle/*.sql` file for these 4 table names combined with "row level"/"policy": zero matches. `drizzle/0023_enable_rls.sql` (the original fix) and `0026_product_leads.sql`/`0027_report_exports.sql` (later tables that got it right) show the established fix pattern. `HANDOFF.md` itself warns: "any *new* table added to `schema.ts` going forward needs its own `ALTER TABLE ... ENABLE ROW LEVEL SECURITY` — Supabase does not inherit this from existing tables, and `check:drift` does not check RLS status, so a new unprotected table would ship silently." `feedback_requests` is a particular concern: it holds `publicToken` (the same value that gates the public feedback-submission endpoint) plus customer ratings/comments and invoice URLs — a table dump would expose every customer's feedback token directly, sidestepping the fact that the token itself is cryptographically random. This finding is based on the committed migrations, matching the code-evidence standard `AGENTS.md` sets for this repo; I attempted the same read-only, count-only anon-key REST probe the team used to confirm the original incident (`Prefer: count=exact`, no row data, against a key that's already public in the browser bundle) but it was blocked by this session's permission classifier as an outbound network call — see chat for details; live confirmation still needs to happen. | Write a migration enabling RLS on all 4 tables with no `CREATE POLICY` statements, matching the default-deny pattern in `0023_enable_rls.sql` exactly (the app's own DB role has `rolbypassrls = true` so Drizzle queries are unaffected). Get explicit approval, apply by hand to the hosted DB per `AGENTS.md` Supabase-safety rules, then re-run `check:drift`. Also worth turning the lesson into a guardrail: `check:drift` doesn't check RLS status today, so this can recur for the next new table too. | `drizzle/0045_enable_rls_missing_tables.sql` is written (mirrors `0023_enable_rls.sql` exactly, no `CREATE POLICY` statements) and is on `main` as of commit `9c366c1`. Applying it to the hosted DB directly (the transactional apply, matching how `0023` was applied) was attempted in-session and blocked by the harness's own permission classifier as a database-mutating action needing a human in the loop — same as the earlier read-only verification attempt. **Still needs a human to run it against the hosted DB**, then re-verify. | Run `drizzle/0045_enable_rls_missing_tables.sql` by hand against the hosted DB in a single transaction (same process as `0023`). Then query `pg_class.relrowsecurity` for the 4 tables (should flip to `true`), and re-run the anon-key REST probe against each to confirm `Content-Range: */0` instead of a real count — same method `HANDOFF.md` used to verify the original fix. | 2026-08-25 |
| Yellow | Open | Customer-facing feedback link is unreachable by its own audience: unauthenticated customers get redirected to `/login` before the page can load. | `src/lib/supabase/middleware.ts`'s `isPublicRoute` allowlist (lines 71-77) covers `/quote/`, `/api/webhooks/`, `/api/public/`, `/api/leads`, `/privacy-policy`, `/terms` but not `/feedback/`. The page at `src/app/feedback/[token]/page.tsx` is a client component that itself only calls the already-public `/api/public/feedback/[token]`, but middleware blocks the page route first. The link is generated (and presumably texted/emailed to customers) by `src/app/api/jobs/[jobId]/feedback-link/route.ts:22` as `/feedback/${token}`. | Add `request.nextUrl.pathname.startsWith("/feedback/")` to the `isPublicRoute` check in `middleware.ts`. | Open a generated feedback link in a signed-out/incognito browser and confirm it renders the rating form instead of redirecting to `/login`. | 2026-08-25 |
| Yellow | Open | Settings section and Supplies have no role gate at the page/layout level — any authenticated non-admin (field employee) can navigate to `/settings/*` or `/supplies` and see the full admin UI shell. | `src/app/(app)/settings/layout.tsx` renders `{children}` for any authenticated user; only the outer `src/app/(app)/layout.tsx` checks for a session (not role). Every other admin section explicitly redirects: e.g. `if (admin.role !== "admin") redirect("/my-day")` in `customers/page.tsx`, `employees/page.tsx`, `payroll/page.tsx`, `reports/page.tsx`, `quality/page.tsx`, `sync-issues/page.tsx`, `leads/page.tsx`, `invoices/page.tsx`, `jobs/page.tsx`, `dashboard/page.tsx`. Data itself is still protected today — `src/app/api/settings/route.ts` GET/PATCH both call `requireAdmin()` — so this is UI-shell exposure, not a data leak, but it's the one admin surface in the app relying entirely on the API layer with no page-level backstop. | Add the same `if (user.role !== "admin") redirect(...)` guard used everywhere else to `settings/layout.tsx` and to `supplies/page.tsx`'s data-fetch path (or a thin server wrapper), so a missing/forgotten `requireAdmin()` on a future settings API route isn't the only thing standing between a field employee and company configuration. | Sign in as a non-admin field employee and confirm `/settings/branding` and `/supplies` redirect instead of rendering. | 2026-08-25 |
| Green | Open | `clock-in`, `transition` (to `traveling`), and `mileage` authorize via `jobAssignments WHERE jobId=? AND userId=?` without an explicit `jobs.companyId = user.companyId` check in that same query — tenant isolation for these three routes depends on the invariant that a `jobAssignments` row can never reference a user outside the job's own company, enforced only at insert time elsewhere. | `src/app/api/jobs/[jobId]/clock-in/route.ts:17-21`, `src/app/api/jobs/[jobId]/transition/route.ts:43-47`, `src/app/api/jobs/[jobId]/mileage/route.ts:16-21` (mileage does join `jobs` but only to read `role`, not to scope). Confirmed the invariant currently holds: every `jobAssignments` insert site (`src/app/api/jobs/route.ts:130`, `src/app/api/jobs/[jobId]/route.ts:92`, `src/app/api/quotes/[quoteId]/convert/route.ts:55`, `src/lib/scheduling/generate-jobs.ts`) validates `employeeIds` against `users.companyId = admin.companyId` before inserting. Not independently exploitable today. | Add `eq(jobs.companyId, user.companyId)` to the join/where clause in these three routes for defense-in-depth, matching the pattern already used in `mileage`'s parent lookup. No behavior change expected since the invariant already holds. | Not required before shipping; low-cost hardening for a future change that adds another `jobAssignments` insert path. | 2026-08-25 |
| Green | Open | Supabase auth session cookie doesn't set `secure: true` explicitly and inherits `httpOnly: false` from `@supabase/ssr`'s own default. | `src/lib/supabase/cookie-options.ts` (`AUTH_COOKIE_OPTIONS` sets only `path`, `sameSite: "lax"`, `maxAge`); confirmed `@supabase/ssr`'s `DEFAULT_COOKIE_OPTIONS` (`node_modules/@supabase/ssr/src/utils/constants.ts`) is `httpOnly: false` by design (the browser client needs to read it) and has no `secure` default either. Not CleanOps-specific — this is the SDK's documented trade-off — and Vercel enforces HTTPS in production, making `secure` moot there today. | Add `secure: true` to `AUTH_COOKIE_OPTIONS` for defense-in-depth (protects any preview/self-hosted deployment not behind forced TLS). `httpOnly: false` is inherent to `@supabase/ssr`'s SSR-hydration model and not worth fighting. | Not required before shipping; cheap hardening, verify cookie still round-trips correctly on a preview deployment after the change. | 2026-08-25 |
| Yellow | Open | No backup/recovery policy is documented anywhere in the repo — `SUPABASE.md` covers local CLI/dev setup only, nothing about the hosted project's backup retention, point-in-time-recovery status, or a tested restore procedure. Not verifiable from code; needs a human to check the Supabase dashboard. | `SUPABASE.md` (full file reviewed) has no backup/recovery section. No `BACKUP.md` or equivalent exists in the repo root. | Check the hosted Supabase project's plan/settings for backup frequency and PITR availability, document the actual policy and last-tested-restore date somewhere in the repo (e.g. a short section in `SUPABASE.md`), and periodically confirm a restore actually works — an untested backup is not a verified backup. | N/A — this is a documentation/process gap, not a code fix. | 2026-08-25 |
| Green | Open | `POST /api/invoices/[invoiceId]/send` has a check-then-act race: it reads `invoice.status !== "draft"` before calling `sendInvoiceViaSquare`, but doesn't flip the status to a "sending" state first, so two near-simultaneous requests (double-click, or two admins) could both pass the check and both attempt to create a Square invoice for the same CleanOps invoice. | `src/app/api/invoices/[invoiceId]/send/route.ts:27-31`; the actual `status: "sent"` update happens inside `sendInvoiceViaSquare` (`src/lib/square/invoicing.ts:53-56`), after the Square API call, not before it. | On the real (non-mock) Square path this is mitigated today by `idempotencyKey: invoice.id` passed to `createAndPublishInvoice` (`src/lib/square/invoicing.ts:46`), which Square's API itself deduplicates on. In mock mode (today's production state) there's no such protection, though the consequence there is at most a redundant mock row, not a real duplicate charge. For defense-in-depth, mark the invoice `status: "sending"` (or similar) in the same request before calling Square, so a concurrent second request's status check fails closed. | Not required before shipping given Square's idempotency key covers the real-money path; worth doing before Square leaves mock mode. | 2026-08-25 |
| Yellow | Open | In Square's current mock mode (today's production state), a real customer who submits feedback after their job gets auto-redirected to a fake, non-functional invoice link — and nothing in the admin invoice UI distinguishes a mock "sent" invoice from a real one, so an admin has no way to know Square isn't actually collecting payment. | Mock invoice URLs are generated as `https://squareup.com/mock-invoice/mock-inv-${invoiceId.slice(0,8)}` (`src/lib/square/client.ts:86-93`) and stored in `invoices.squarePublicUrl` whenever an admin clicks "Send invoice" while Square is unconfigured (`src/lib/square/invoicing.ts:53-56`). That same URL is handed to the customer as `invoiceUrl` by the public feedback endpoint (`src/app/api/public/feedback/[token]/route.ts:15`), and the feedback page auto-navigates there on submit (`src/app/feedback/[token]/page.tsx:23`: `window.location.assign(body.invoiceUrl)`) — a real customer lands on a dead Square URL. Separately, the admin invoice detail page (`src/app/(app)/invoices/[invoiceId]/page.tsx:296`) shows only `invoice.method` ("square") with no mock/real distinction; the only place mock state is visible at all is the separate Settings → Integrations page, which an admin sending an invoice has no reason to check. | Don't set `squarePublicUrl` to a fake external domain in mock mode — either omit it (customer flow already handles `invoiceUrl: null` gracefully, showing "Your response has been sent to the team" instead of "continue to payment") or point it at an internal "payment coming soon" page. Separately, surface mock state on the invoice/quote UI itself (e.g. a small "Square is in test mode — no real charge occurred" badge) wherever `method: "square"` is shown, not just on the Settings page. | After the fix, send a mock invoice, submit feedback as that customer, and confirm no broken external redirect happens; confirm the admin invoice view visibly indicates mock mode. | 2026-08-25 |
| Yellow | Open | Inbound webhook processing errors (GHL lead-capture webhooks and Square payment webhooks) are recorded to the database but never surfaced in any admin-facing UI — unlike outbound GHL syncs, which have a dedicated `/sync-issues` page. | `src/app/api/webhooks/ghl/route.ts` and `src/app/api/webhooks/square/route.ts` both write failures to `webhookEvents.error` (e.g. "Payload has no usable contact fields", "No invoice found for Square invoice X"), but `src/app/(app)/sync-issues/page.tsx:5` only queries `ghlSyncLog` (outbound syncs) — it never reads from the `webhookEvents` table at all. A failed inbound Square payment webhook (e.g. a race where the webhook arrives before `squareInvoiceId` is persisted) would leave an invoice permanently unpaid-looking with zero indication anywhere in the app that anything went wrong. | Extend `/sync-issues` (or add a companion view) to also surface `webhookEvents` rows with a non-null `error`, scoped by company where the row has one (GHL lead-capture events aren't company-scoped pre-processing, so may need a separate "system" section). | Trigger a deliberately-malformed webhook locally and confirm it now shows up somewhere in the admin UI, not just in the raw `webhook_events` table. | 2026-08-25 |

## Resolved / not reproducible

| Priority | Status | Finding | Evidence | Remediation / next action | Validation | Updated |
|---|---|---|---|---|---|---|
| Red | Fixed (`main` `9c366c1`) | Square webhook signature verification failed **open**: if no Square webhook signing key was configured anywhere (the current production state per `AI-NOW.md` — "Square invoicing still mock in prod"), every incoming webhook was accepted with no signature check, and could mark any invoice `status: "paid"` on a forged `invoice.payment_made` event. Separately, a valid signature from any one company's key authenticated a forged payment webhook for a different company's invoice, since the fallback validation and invoice lookup weren't scoped per-company. | Was `src/app/api/webhooks/square/route.ts:21`: `signatureValid = webhookKeys.length === 0 \|\| webhookKeys.some(...)` — comment named it "today's permissive mock behavior". `getSquareWebhookKeys()` pooled every company's key into one flat list. | Now fails closed: a configured key must actually match; the no-key fallback only applies when `process.env.NODE_ENV !== "production"`. `getSquareWebhookKeys()` now returns each key's owning `companyId`, and once a key matches, the invoice lookup is scoped to that company (`and(eq(invoices.squareInvoiceId, ...), eq(invoices.companyId, matchedCompanyId))`) when the match came from a company-owned key. | Verified locally against a production build (`npx next start`, `NODE_ENV=production`): a forged webhook with no signature and no key configured now returns `401 Unauthorized` (previously would have returned `200`/accepted). `npm run verify`, `smoke:routes` (6/6), `smoke:auth` (18/19, the 1 failure is a pre-existing unrelated `/supplies` marker mismatch) all green. | 2026-08-25 |
