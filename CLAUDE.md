@AGENTS.md
@HANDOFF.md

Before making any change, read `AGENT-COLLABORATION.md` and `AGENTS.md`.

  Work only in your dedicated worktree (`cleanops-claude`), never directly in
  `cleanops-v1/main`, unless explicitly assigned an integration task.